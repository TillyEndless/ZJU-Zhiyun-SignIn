ZJU_USERNAME=3240100000
ZJU_PASSWORD=your_passw0rd

# For pintia.cn (Optional, used by reliableTodolist.js)
PINTIA_EMAIL=your_email@example.com
PINTIA_PASSWORD=your_pintia_password

# For DingTalk notification(Optional)
ENABLE_DINGTALK=true # set to anything except "true" to disable
DINGTALK_WEBHOOK="https://oapi.dingtalk.com/robot/send?access_token=your_access_token"
DINGTALK_SECRET="SECyour_secret_token"

# autosign.js：签到事件本地日志路径（可选，默认 courses.zju/autosign-events.log）
# AUTOSIGN_EVENT_LOG=/path/to/autosign-events.log
# 无点名时终端心跳间隔（毫秒），默认 60000；0 = 终端不再打印 No rollcalls
# AUTOSIGN_EMPTY_CONSOLE_MS=60000
# 连续拉取点名失败多少次才钉钉提醒（默认 24）；网络抖动时避免误报。0=不因拉取失败发钉钉
# AUTOSIGN_FETCH_FAIL_DING_AFTER=24
# 设为 false 关闭「拉取点名列表失败」类钉钉（仍会写 autosign-events.log）
# AUTOSIGN_FETCH_FAIL_DING=false